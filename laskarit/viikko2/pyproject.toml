[tool.poetry]
name = "unicafe"
version = "0.1.0"
description = "Tehtävä4"
authors = ["kety <kety.kuusaru@helsinki.fi>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
